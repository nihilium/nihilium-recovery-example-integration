// The vendored bundle is plain JS with no types of its own. It is imported for its side effect —
// assigning `window.NihiliumDS` — and the components are typed in `../ds-types.ts` instead.
export {};
