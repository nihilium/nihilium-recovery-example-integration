# The template. `npm install` copies this to .env.local and fills in the generated secrets — edit
# that file, not this one. Everything else has a working default: the app runs in simulated mode
# against public RPCs with no configuration at all.

# simulated (default) | live.  live is PAID and blocks on a human answering an email.
VITE_RECOVERY_MODE=simulated

VITE_SERVER_URL=http://localhost:8787
VITE_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
VITE_BUNDLER_URL=https://public.pimlico.io/v2/11155111/rpc

# Generated per machine by scripts/setup-env.mjs, and matched in server/.env. These are write
# capabilities — appending to a record, registering a watch — so they are not values a repository
# can ship. Reading a record needs neither: the record id is the read capability.
VITE_RECORD_APPEND_SECRET=
VITE_WATCH_REGISTER_SECRET=

# Live mode only. This key reaches the browser — an accepted demo trade-off (it is a spend limit on
# sealing, not access to funds). A real wallet keeps its payment provider behind its own backend.
VITE_NIHILIUM_API_KEY=
VITE_NIHILIUM_API_URL=https://api.nihilium.io
VITE_NIHILIUM_EMAIL_SERVICE_URL=https://zkemail.nihilium.io
